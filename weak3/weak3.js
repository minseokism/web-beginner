/*
javascript overloading 없다


*/


var obj= {};  // 주로 리터럴 방식으로 생성한다. [리터럴로 만듬 ]

var obj2={
	name : 'ddd',
	age : 10
};
obj.job = 'hi'; // 이런식으로 추가한다. 


var obj3 = new Object() // new 를 이용해서 만들지 않는다 [생성자로 만듬 ]

/////////////////////////////////////

for( var j in arr){
	//arr에 있는 속성까지 다 출력 (사용자가 입력한 값만출력 , 기본 속성들은 출력 안함)
}

